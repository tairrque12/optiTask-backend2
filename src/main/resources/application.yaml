spring:
  datasource:
    url: jdbc:postgresql://localhost:5435/optitask
    username: optitask
    password: optitask
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration